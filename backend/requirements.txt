# Core (pinned to versions with prebuilt wheels for Python 3.11–3.14).
# The previous pins (pydantic 2.8.2 etc.) had no wheels for 3.13+ and failed to
# build from source. Use `>=` lower bounds so it installs across modern Pythons.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0
bcrypt>=4.0            # password hashing for email/password auth
cryptography>=42.0     # encrypt linked GitHub tokens at rest (Fernet)
aiosmtplib>=3.0,<5.0   # transactional email (verification, reset, login OTP)

# Optional — headless-browser DOM rendering (services/renderer.py). Lets DOM
# grounding verify against the real, JavaScript-rendered DOM of SPA apps instead
# of declining their empty shell. Entirely optional: without it (or without a
# browser binary) rendering falls back to the static fetch and the product runs
# unchanged. To enable, uncomment the pin and install the browser binary:
#     pip install playwright && playwright install chromium
# playwright>=1.40

# Dev / test
pytest>=8.0
